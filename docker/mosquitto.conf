# Minimal broker config for the Cypress MQTT e2e test.
# Anonymous access on all interfaces so both the wavelog-web container
# (via the docker network alias "mqtt-broker") and the Cypress node process
# (via the published host port) can connect.
listener 1883 0.0.0.0
allow_anonymous true
